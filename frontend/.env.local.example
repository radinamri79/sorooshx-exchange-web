# ============================================
# SorooshX Frontend Environment Variables
# Copy this file to .env.local for local development
# For Vercel: Set these in Vercel Dashboard → Settings → Environment Variables
# ============================================

# Backend API URL (Required)
# For Vercel with separate backend: https://api.your-domain.com/api/v1
# For development: http://localhost:8000/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# WebSocket URL for backend (Required for order updates)
# For Vercel with separate backend: wss://api.your-domain.com/ws
# For development: ws://localhost:8000/ws
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# Binance WebSocket for real-time market data
# This is the public Binance Futures WebSocket endpoint
# Keep this as-is unless you have a proxy
NEXT_PUBLIC_BINANCE_WS_URL=wss://fstream.binance.com
